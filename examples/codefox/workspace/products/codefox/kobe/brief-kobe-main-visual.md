# Kobe Main Visual Palette Refresh

Create a launch marketing hero for kobe, an SSH-friendly terminal IDE for
running parallel AI coding agents.

Use the reference image at `workspace/products/codefox/kobe/references/main_visual.png` as the visual
palette and composition language: dark ink blocks, cream editorial canvas, mint
routing traces, amber highlights, coral task/status accents, crisp black
outlines, and restrained grid structure.

Keep the real kobe TUI product as the subject. The output should still read as a
terminal-native product, not a generic SaaS dashboard or decorative abstract
poster.

Extracted reference palette:

- `#101820` ink black
- `#1A2632` panel navy
- `#F7F1E5` warm cream
- `#FFFCF5` soft cream
- `#2FE6A7` mint green
- `#FFB000` amber
- `#E84A5F` coral
- `#748490` muted slate
