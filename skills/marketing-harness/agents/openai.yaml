interface:
  display_name: "Marketing Harness"
  short_description: "Validate, render, publish, and package brand-locked marketing assets."
  default_prompt: "Use $marketing-harness to operate the marketing image-generation harness."

policy:
  allow_implicit_invocation: true
